const arr = ['зеленый', 'желтый', 'красный'];
for (var i = 0; i< arr.length; i++) {
   if (arr[i] === 'зеленый') {
      console.log('идти');
   } else if (arr[i]=== 'желтый') {
      console.log('жди');
   } else if (arr[i]=== 'красный') {
      console.log('стой');
  }
}
